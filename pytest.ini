[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers
markers =
    integration: Integration tests that require Home Assistant instance
    unit: Unit tests that don't require external dependencies

# Asyncio mode for async tests
asyncio_mode = auto
